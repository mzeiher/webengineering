var nbr = 0;
var str = "new string literal";
var str = 'single quotes are also string literals';
var str = `multi line text ${nbr} with placeholders
can bne written with a string literal`;
var str = new String("string object");
